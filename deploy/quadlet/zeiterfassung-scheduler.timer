# Diese Datei gehoert NICHT in das Quadlet-Verzeichnis, sondern zu den
# gewoehnlichen systemd-Units:
#   rootless: ~/.config/systemd/user/
#   als root: /etc/systemd/system/
# Quadlet erzeugt aus zeiterfassung-scheduler.container die Unit
# zeiterfassung-scheduler.service, die dieser Timer startet.
[Unit]
Description=Vergessene Stempelungen stuendlich beenden

[Timer]
OnCalendar=hourly
# Nicht alle Installationen zur vollen Stunde gleichzeitig.
RandomizedDelaySec=5m
# Einen Lauf nachholen, der waehrend einer Auszeit des Servers ausgefallen ist.
Persistent=true
Unit=zeiterfassung-scheduler.service

[Install]
WantedBy=timers.target
